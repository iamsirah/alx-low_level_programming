#include <stdio.h>
/**
 * main - Entry point
 * Description: printf
 * Return: always 0 (success)
 */

int main(void)
{
	char string[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", string);
Return: (0);
}
